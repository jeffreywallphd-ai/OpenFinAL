import type * as Types from '../IKeylessModelGateway';

describe('IKeylessModelGateway', () => {
  it('type module compiles', () => {
    const ok: boolean = true;
    expect(ok).toBe(true);
  });
});
